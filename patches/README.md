# Downstream source patches

Store reviewed changes as `<source-id>/<name>.patch` and list their ordered paths
and SHA-256 hashes in `source-lock.json`. See [the workflow](../docs/source-patches.md).
There are no production bot-source patches in this scaffold yet.
